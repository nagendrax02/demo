const UPLOAD = 'Upload';
const DELETE_FILE = 'Delete File';
const TEST_FILE = new File(['test'], 'test.png', { type: 'image/png' });
const DEFAULT_FILE_SIZE_LIMIT = 10;

export { UPLOAD, DELETE_FILE, TEST_FILE, DEFAULT_FILE_SIZE_LIMIT };
