import { BgColor } from '../../types';
import StyledIcon from '../shared/styled-icon';

const Icon = (): JSX.Element => {
  return <StyledIcon name="timeline" bgColor={BgColor.Blue500} />;
};

export default Icon;
