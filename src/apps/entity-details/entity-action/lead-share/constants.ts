import { GroupConfig } from '../../../../common/component-lib/grouped-option-dropdown';

export const LEAD_SHARE_GROUP = 'results';

export const DEFAULT_MESSAGE_LENGTH = 1000;

export const SEARCH_LENGTH = 3;

export const LEAD_SHARE_GROUP_CONFIG: GroupConfig = {
  [LEAD_SHARE_GROUP]: {
    showSecondaryLabelOnSelect: true
  }
};
